server.basePath: "/kibana"
server.rewriteBasePath: true
server.host: "0.0.0.0"
logging.dest: /Users/aa/private/k.log

elasticsearch.hosts: ["http://localhost:9200"]

kibana.index: ".kibana"
kibana.defaultAppId: "home"

elasticsearch.username: "aa"
elasticsearch.password: "iheartfly"
